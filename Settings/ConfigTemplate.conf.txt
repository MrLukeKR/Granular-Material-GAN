IO_DATA_ROOT_DIR="/directory/of/your/data/"
IO_OUTPUT_ROOT_DIR="/directory/of/your/outputs/"

PREPROCESSING_BINARY_THRESHOLD=0.95

VOXEL_DIMENSION=64

# Method used to resolve non-perfect voxel division {LOSSY, PADDING, STRETCH, SHRINK, EXTRAPOLATE}
VOXEL_RESOLVE_METHOD="LOSSY"

# --- TRAINING OPTIONS ---

TRAINING_BATCH_SIZE=8
TRAINING_EPOCHS=10
TRAINING_EPOCH_STEPS=500

# ------------------------

# --- ENABLE/DISABLE FEATURES ---

# Enable saving of plots to disk, useful if no graphical display is available
ENABLE_IMAGE_SAVING=True

# Enable the showing of images, required to be FALSE if no graphical display is available (i.e. through SSH)
ENABLE_IMAGE_DISPLAY=False

# Enable the use of Keras (i.e. Machine Learning), recommended to be FALSE if using a non-specialist machine
ENABLE_SEGMENTATION=True

# Enable the image processing of input data, may improve performance of Machine Learning
ENABLE_PREPROCESSING=True

# -------------------------------